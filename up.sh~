#!/bin/bash

up() {
    if [ -z "$1" ] 
    then 
        GO_UP=1
    else
        GO_UP="$1" # find better way to grab default port
    fi

    for i in `seq 1 $GO_UP`; do cd ..; done
   
    
}

up 2
